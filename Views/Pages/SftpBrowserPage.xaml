<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wormhole.Views.Pages.SftpBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Local" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock Text="Local file list will render here."
                           Style="{StaticResource BodyTextBlockStyle}"
                           Opacity="0.7" />
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <Grid Grid.Column="2" Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Remote" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock Text="Remote file list will render here via SSH.NET SftpClient."
                           Style="{StaticResource BodyTextBlockStyle}"
                           Opacity="0.7" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
