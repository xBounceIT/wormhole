# Build outputs
bin/
obj/
artifacts/
*.binlog

# Visual Studio
.vs/
*.user
*.suo

# Rider / VS Code
.idea/
.vscode/
*.swp

# Generated WinUI files
*.g.cs
*.g.i.cs
*.xbf
*.pri

# Logs
logs/
*.log

# Local database files (developer scratch only; runtime DB lives in %LOCALAPPDATA%)
*.db
*.db-shm
*.db-wal

# Installer outputs
installer/output/


# Fetched web assets (xterm.js etc.) -- populated by scripts/Fetch-WebAssets.ps1
Assets/web/vendor/

# Go sidecar build outputs. go.sum is uncommitted because the .NET build is the
# source of truth (Fetch-*Proxy.ps1 regenerates via `go mod download all`).
tools/*/go.sum

# C++ shim build outputs from CMake + vcpkg manifest mode.
tools/wormhole-ovpnproxy/ovpn_shim/build/

# OS noise
Thumbs.db
desktop.ini
.DS_Store
.claude/
